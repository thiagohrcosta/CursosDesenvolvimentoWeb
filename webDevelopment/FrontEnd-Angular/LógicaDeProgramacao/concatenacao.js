/* CONCATENAÇÃO

    É o termo usado em computação para designar a operação
    de unir o conteúdo de duas strings

*/

const nome = 'José'
const sobrenome = 'da Silva'
const nomeCompleto = (`${nome} ${sobrenome}`)
console.log(nomeCompleto)